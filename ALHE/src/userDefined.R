# TODO: Add comment
# 
# Author: Przemo
###############################################################################


# funkcja definiujaca zakresy mozliwych wartosci dla kazdego wymiaru
generateLimits = function() {
    limitDim1 = list(left=-3, right=4);
    limitDim2 = list(left=-2, right=5);
    limits = list(limitDim1, limitDim2);
    return(limits);
}
